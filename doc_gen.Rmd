---
title: "Test report"
output:
  word_document:
    reference_docx: ref_doc.docx
---

<!-- Setup and library calls -->
```{R setup, include=FALSE}
knitr::opts_chunk$set(warning = FALSE, message = FALSE, fig.width = 180, fig.height = 10)
library(knitr)
library(ggplot2)
library(ggthemes)
library(ggpubr)
library(tidyverse)
library(extrafont)
library(pander)
library(haven)
library(flextable)
# source("processor.R")
source("graph_table_process.R")
# font_import("Calibri")
```


```{R Graphing, echo=FALSE}
load("processedData.RData")
out = NULL

ct
for (qData in processed_graph_dataList) {
  if(qData[[1]] == "mx"){
    mx_graph(qData)
    mx_table(qData)
  }
  else if(qData[[1]] == "mc"){
    mc_graph(qData)
    mc_table(qData)
  }
  else if(qData[[1]] == "ms"){
    ms_graph(qData)
    ms_table(qData)
  }
  else if(qData[[1]] == "mx.tri"){
    mx.tri_graph(qData)
    mx.tri_table(qData)
  }
  else if(qData[[1]] == "mc.yn"){
    
  }
}
```



`r paste(knit(text = out), collapse = '\n')`



